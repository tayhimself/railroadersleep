---
import Item from '~/components/blog/ListItem.astro';

const { posts, preference="image" } = Astro.props;
---

<ul>
	{
		posts.map((post) => (
			<li class="mb-10 md:mb-16">
				<Item post={post} preference={preference} />
			</li>
		))
	}
</ul>
