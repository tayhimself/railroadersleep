---
import { findImage } from "~/utils/images";

const { item } = Astro.props;

const image = await findImage(item.image);

---
<article class="max-w-md mx-auto md:max-w-none grid md:grid-cols-2 gap-6 md:gap-8 my-8 lg:my-12 items-center">
	<a class="relative block group" href={item.link}>
		<div
			class="relative h-0 pb-[56.25%] md:pb-[75%] md:h-72 lg:pb-[56.25%] overflow-hidden rounded shadow-lg"
		>
    {image && (
			<img
				src={image.src}
				class="absolute inset-0 object-scale-down w-full h-full mb-6 rounded shadow-lg"
				alt={item.title}
			/>
    )}
    </div>
	</a>
	<div>
		<header>
			<h2 class="text-lg sm:text-xl font-bold leading-snug mb-2 font-heading">
				<a
					class="hover:text-primary-600 underline underline-offset-4 decoration-1 decoration-dotted transition ease-in duration-200"
					href={item.link}
				>
					{item.title}
				</a>
			</h2>
      <p class="text-sm sm:text-base mb-4">{item.description}</p>
		</header>

		<footer class="mt-4">
		</footer>
	</div>
</article>